# Session — Phase 1 Kickoff

## What was built
- Full project scaffold generated by setup_clinicflow.sh
- CLAUDE.md with complete project context for Claude Code
- All 7 docs written: product brief, architecture, schema, API contracts,
  risk scoring rubric, LLM prompts, tech decisions
- Backend skeleton: FastAPI app, config, database module, health endpoint
- Docker Compose: Postgres 15, Redis 7, MinIO, backend, worker
- GitHub Actions CI pipeline
- pyproject.toml with all dependencies

## Decisions made
- Arq over Celery for async workers
- Shared DB + Postgres RLS for multi-tenancy
- gpt-4o-mini for SMS, gpt-4o for analysis
- Magic links for patient auth

## Next session — Phase 2: Auth
1. Create SQLAlchemy models: tenants, users, refresh_tokens
2. Write Alembic migration (review before applying)
3. Implement JWT helpers in app/core/security.py
4. Build /auth/login and /auth/refresh endpoints
5. Write FastAPI dependency get_current_user() that sets RLS tenant context
6. Tests: login success, wrong password, token refresh, token rotation

## Open questions
- Confirm Stripe price IDs once Stripe account is set up
- Decide on email provider for magic links (Resend vs SendGrid vs SES)
